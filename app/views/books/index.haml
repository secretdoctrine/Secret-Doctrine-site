= render partial: 'partials/contents_tree_block', :locals => { :display_contents => false, :tree => @tree }

%script{:type => "text/javascript"}
  $('#tree_container_div').jstree({
  "core" : {"themes" : {"icons" : false}}
  });

%div{:id => "main_container_div", :class => "fullheight"}
  this id main container


= render partial: 'partials/book_view_resize_script'