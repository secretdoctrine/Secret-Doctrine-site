%div{class: 'snippet_result'}
  = link_to (page_counter + 1).to_s + '. ' + page.full_path_name, refinery.books_book_page_path(page.book.id, page.url_name, highlight_words: Refinery::Books::WordsConverter.convert(page.words.page_text))
  = link_to refinery.export_books_book_page_path(page.book.id, page.url_name), class: 'but_24' do
    %span.s0
      %span.s1
        %span.s2
          = I18n.t('library.search_page.export_page')
  %br
  = page.excerpts.page_text.html_safe