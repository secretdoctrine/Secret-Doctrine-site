%aside.left
  = render :partial => 'player'
  %h2
    Каталог
  %nav{class: "menuleft navmenu", style: "margin-top: 0px;"}
    %div.multimedia_jstree_div
      %ul
        = render :partial => 'tree_group_li', :collection => root_multimedia_groups, :as => :group, :locals => {:current_group => current_group}
  :javascript
    $('.multimedia_jstree_div').jstree({
      "core" : {"themes" : {"icons" : false}}
    });

    $('.multimedia_jstree_div').on("activate_node.jstree", function(e,data){
      window.location.href = data.node.a_attr.href;
    })
  %section.how_to_use
    = raw page.content_for(:body)