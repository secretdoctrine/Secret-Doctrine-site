%li
  = link_to group.title, refinery.multimedia_groups_multimedia_group_path(group.id)
  - if group.multimedia_groups.any?
    %ul
      = render :partial => 'tree_group_li', :collection => group.ordered_groups, :as => :group
