= stylesheet_link_tag 'multimedia.css'

%div.mainclear
  = #render :partial => 'multimedia_group', :collection => @root_multimedia_groups, :as => :multimedia_group
  %div.maincontent
    %div.content
      %article.bodycontent
        = raw @page.content_for(:side_body)
  = render :partial => 'left_bar', :locals => {:page => @page, :root_multimedia_groups => @root_multimedia_groups, :current_group => nil}