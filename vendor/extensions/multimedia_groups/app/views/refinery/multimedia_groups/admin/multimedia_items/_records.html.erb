<%= render 'refinery/admin/search_header', :url => refinery.multimedia_groups_admin_multimedia_items_path %>
<div class='pagination_container'>
  <% if @multimedia_items.any? %>
    <%= render 'multimedia_items' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
